setup:
  config:
    API_URL: https://hokela-api.herokuapp.ca
build:
  docker:
    web: Dockerfile.web
  config:
    API_URL: https://hokela-api.herokuapp.com
# run:
#   web: bundle exec puma -C config/puma.rb